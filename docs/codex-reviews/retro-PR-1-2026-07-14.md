# Retro Codex Review — PR #1 (merged) — 2026-07-14

## メタ情報

| 項目 | 値 |
|---|---|
| 実施日時 | 2026-07-14 (JST) — merge 後 retro review |
| Thread ID | `019f60c4-ba92-7bd2-b4eb-5c79f6d06046` |
| 使用モデル | gpt-5.5 (`~/.codex/config.toml`) |
| Base → Head | `171434ba` → `e06efdee` (PR #1 merge commit) |
| 規模 | 8 files, +37 / −11 |
| スコープ | security / injection / correctness / Next.js build-time env inlining / missed call sites |
| 経緯 | PR #1 はオーナー指示で codex review をスキップして直マージ（個人 fork）。ポリシーの retro 条項に基づく 24h 以内の遡及レビュー |

## 対象コミット

1. `9dc7b121` fix(slim/web): escape transcript-derived content before innerHTML (XSS)
2. `634a274f` feat(terminal): make the bot display name build-time configurable (NEXT_PUBLIC_BOT_NAME)

## Punchlist

- **P0**: なし
- **P1**: なし
- **P2**: なし

## 領域別所見（clean の根拠）

- **Slim XSS fix**: 変更対象の `innerHTML` sink を網羅（web.py:127, 136, 148, 151, 152, 156）。attribute context は quoted + escaped、event handler は static、URL は `encodeURIComponent` 維持、text-only 経路は `textContent`。double-escaping なし。
- **Bot name injection**: `botName()` は `JSON.stringify` 経由で `POST /api/bots` へ。この diff に HTML / URL / attribute / event-handler sink なし。
- **Next.js env inlining**: `botName.ts:10` は `process.env.NEXT_PUBLIC_BOT_NAME` の直接参照で静的置換可能。全呼び出しはクライアント側。
- **取り残し**: 本番 5 経路すべて `botName()` 化済み（meeting.tsx:329, 652 / meetingCookbook.ts:47 / meetingPrep.tsx:242 / meetingsOnboarding.tsx:149）。残存 `"Vexa"` はテスト・ブランディング・slim 側デフォルトのみで意図どおり。
- **Docker/compose 配線**: Dockerfile:37 で `npm run build` 前に ARG 設定、compose は `${BOT_NAME:-}`。空デフォルトで既存挙動維持。

**Verdict: clean for the requested diff scope.**

## 是正状況

指摘ゼロのため是正なし。

## Handoff / follow-up

- NEXT_PUBLIC_BOT_NAME の設定化は upstream (Vexa-ai/vexa) への contribution 候補 — issue 起票時はこのレビューを添付可
