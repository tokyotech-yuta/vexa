node_modules/
dist/
.turbo/
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
*.log
.DS_Store
.env
.env.*
!.env.example
!**/.env.example
.recordings/
deploy/execution-targets.json
# local test/debug artifacts (Playwright MCP screenshots + snapshots)
.playwright-mcp/
/*.png
# local Claude Code settings + worktrees (per-machine, never committed)
.claude/
# generated architecture perspectives (regenerate: pnpm arch:viz) — keep the README, ignore the SVGs
docs/views/*
!docs/views/README.md
# the concise DSL projection IS committed — it's the always-in-context LLM index (gate:dataflow keeps it current)
!docs/views/architecture.dsl
# the Mermaid views are committed too — deterministic projections of the chart (pnpm arch:dsl --write; gate:dataflow checks staleness)
!docs/views/*.mmd

# transcription service downloaded model weights (runtime cache)
deploy/transcription/models/

# Release working handoff notes are local-only (per-version); witness.json stays tracked.
releases/*/HANDOFF.md
